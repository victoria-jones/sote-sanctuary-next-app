export default function Hero () {
    return(
        <section className="hero">
            <div>salt of the earth sanctuary logo</div>
            <div>hero bg image</div>
        </section>
    );
}